let pixKey: string | number;

// ? email
pixKey = 'teste@teste.teste';

console.log(pixKey);

// ? phone
pixKey = 54987654321;

console.log(pixKey);

// ? cpf
pixKey = '01234567890';

console.log(pixKey);
